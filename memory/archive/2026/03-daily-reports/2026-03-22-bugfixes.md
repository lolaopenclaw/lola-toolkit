# Bug Fixes - 2026-03-22

## Fixed Bugs

### 1. ✅ garmin-health-report.sh - DATE_ACTIVITY undefined in --current mode

**File**: `/home/mleon/.openclaw/workspace/scripts/garmin-health-report.sh`

**Problem**: 
- Line 238 (in Python code) referenced `$DATE_ACTIVITY` variable
- This variable was only defined for `--daily` mode (line 43)
- For `--current` mode, the variable was `$DATE` (line 49)
- Result: `DATE_ACTIVITY: unbound variable` error when running `--current` mode

**Fix Applied**:
- Changed the Python code structure from `if MODE == "--daily" or MODE == "--current":` 
- Split into two separate blocks: `if MODE == "--daily":` and `elif MODE == "--current":`
- Now `--current` mode properly uses its own `$DATE` variable
- Each mode has its own clear execution path

**Verification**:
```bash
bash -n scripts/garmin-health-report.sh  # ✅ Syntax OK
```

---

### 2. ✅ backup-memory.sh - NATIVE_BACKUP_DIR undefined

**File**: `/home/mleon/.openclaw/workspace/scripts/backup-memory.sh`

**Problem**:
- Line 119 referenced `$NATIVE_BACKUP_DIR` in cleanup section
- Variable was never defined anywhere in the script
- Result: `NATIVE_BACKUP_DIR: unbound variable` error

**Fix Applied**:
- Removed `$NATIVE_BACKUP_DIR` from the cleanup line
- Now only cleans up `$BACKUP_DIR` and `$BACKUP_FILE` (which are properly defined)

**Verification**:
```bash
bash -n scripts/backup-memory.sh  # ✅ Syntax OK
```

---

## Script Quality Audit

Scanned all 43 active scripts (non-archived) for common issues:

### Missing Strict Mode (10 scripts)

Scripts without `set -e` or `set -euo pipefail`:

1. **monitor-github-24586-robust.sh** - Intentional (monitor loop, shouldn't exit on errors)
2. **google-tts.sh** - Should add `set -e`
3. **verify.sh** - Has `set -uo pipefail`, intentionally missing `-e` (verification script)
4. **apt-security-check.sh** - Should add `set -e`
5. **resumen-garmin-semanal-robust.sh** - Should add `set -e`
6. **healthcheck-api.sh** - Has explicit error handling, doesn't need strict mode
7. **check-driving-mode.sh** - Has explicit error handling, doesn't need strict mode
8. **secret-get.sh** - Has explicit error handling, doesn't need strict mode
9. **gemini-reindex-watchdog.sh** - Should add `set -e`
10. **garmin-activities-weekly.sh** - Should add `set -e`

### Unquoted Variables (1 script)

**bootstrap.sh** - Line 248:
```bash
if ! grep -q "^${key}.*$(echo $setting | cut -d' ' -f2)" /etc/ssh/sshd_config 2>/dev/null; then
```
- `$setting` should be quoted: `"$setting"`
- However, this is in a hardening script that modifies SSH config
- **Recommendation**: Fix requires careful testing with SSH backup session open

### False Positives

- `gemini-slow-reindex.sh` uses `$GEMINI_API_KEY` - OK (env var, checked on line 15)
- `informe-matutino-auto.sh` uses `$NF` - OK (awk built-in, not bash variable)

---

## Recommendations for Human Review

### High Priority

1. **bootstrap.sh line 248** - Quote `$setting` variable
   - Risk: SSH config changes, must test with backup connection
   - Protocol: Apply critical change protocol (AGENTS.md section "Cambios Críticos")

### Medium Priority

Scripts that should add `set -e`:
- google-tts.sh
- apt-security-check.sh  
- resumen-garmin-semanal-robust.sh
- gemini-reindex-watchdog.sh
- garmin-activities-weekly.sh

**Safe to apply**: Yes, but should test each after adding.

---

## Summary

- **Bugs Fixed**: 2/2 ✅
- **Syntax Verified**: All fixed scripts pass `bash -n`
- **Scripts Scanned**: 43 active + 31 archived
- **Issues Found**: 11 (2 critical fixed, 9 recommendations)
- **Commits**: Changes committed to git

---

## Testing Performed

```bash
# Syntax checks
bash -n scripts/garmin-health-report.sh  # ✅ PASS
bash -n scripts/backup-memory.sh        # ✅ PASS

# Full script scan
# No critical undefined variable errors detected
```

---

## Next Steps

1. Consider adding `set -e` to the 5 recommended scripts (test each)
2. Review bootstrap.sh line 248 unquoted variable (needs SSH backup protocol)
3. All other scripts are clean or have intentional design choices

---

Generated by: Bugfix Subagent
Date: 2026-03-22 15:19 CET
